#include <stdio.h>
#include <conio.h>

int main()
{
    // it will once we set up the correct path for mingw
    printf("Hello Bru!");
    printf("Does this work?");
    printf("oww yeah"); 
    printf("but how to run code?");
    int someVar = 0;
    scanf("enter something %d", &someVar);

    // for that you need to go through that link
    // there it is specifed how you will run code
    // can you open a cpp properties json file?
    // and update the path there
    return 0;

}
